import { defineComponent } from "vue";

const Workbench = defineComponent({
    setup() {
        return () => {
            return (
                <div>workbench</div>)
        }
    }
})

export default Workbench